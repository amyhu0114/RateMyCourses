<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Make Review</title>
    <!-- load local stylesheet (css) -->
    <link rel="stylesheet" href="/styles.css" />
    <!-- loading jquery -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</head>
<body>
   <%- include("partials/navbar.ejs", {loggedIn: loggedIn}) %>
   <%- include("partials/flashes.ejs") %>
    <h1>Submit a Review</h1>

<form id="inputCourse"
      method="POST"
      action="/inputCourse/">
     <label>Course ID: </label> 
     <input placeholder=123" name="courseId"><br><br>
     <label>Course Name: </label> 
     <input placeholder="Databases with Web Interfaces" name="courseName"><br><br> 
     <label>Course Code: </label> 
     <input placeholder="CS304" name="courseCode" id = "courseCode"><br><br>
     <label>Department: </label> 
     <Select name = department>
        <% departments.forEach( (department) => {%>
            <option value = <%= department.departmentId %>><%= department.departmentName %></option>
        <% }); %>
     </Select><br><br>
  <p><button type="submit">Submit</button></p>
</form>
<script>
    submit.addEventListener("click", validate);

    function validate(e) {
        e.preventDefault();

        const courseCode = document.getElementById("courseCode");
        let valid = true;

        if (courseCode.value.indexOf(" ")!=-1) {
            req.flash('error', 'Invalid stuff');
        }
        return valid;
    }
</script>

    <!-- load local js -->
    <script src="/main.js"></script>
</body>
</html>